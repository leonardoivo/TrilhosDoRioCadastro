-- ESIM.dbo.ITEMMOVTORETORNOBOLETO definition

-- Drop table

-- DROP TABLE ESIM.dbo.ITEMMOVTORETORNOBOLETO GO

CREATE TABLE ESIM.dbo.ITEMMOVTORETORNOBOLETO (
	ITEMMOVTORETORNOID int NOT NULL,
	ITEMMOVTOCOBRANCAID int NULL,
	DATAPAGAMENTO date NULL,
	VALORDOCUMENTO numeric(14,2) NULL,
	OUTRASDEDUCOES numeric(14,2) NULL,
	MORAMULTA numeric(14,2) NULL,
	OUTROSACRESCIMOS numeric(14,2) NULL,
	NOSSONUMERO varchar(20) COLLATE Latin1_General_CI_AI NULL,
	DESCONTO numeric(14,2) NULL,
	ABATIMENTO numeric(14,2) NULL,
	DATACREDITO date NULL,
	NUMERODOCUMENTO varchar(12) COLLATE Latin1_General_CI_AI NULL,
	CONSTRAINT PK_ITEMMOVTORETORNOBOLETO PRIMARY KEY (ITEMMOVTORETORNOID)
) GO
 CREATE NONCLUSTERED INDEX IX_FK_ITEMMOVTRB_ITEMMOVTCB ON dbo.ITEMMOVTORETORNOBOLETO (  ITEMMOVTOCOBRANCAID ASC  )  
	 WITH (  PAD_INDEX = OFF ,FILLFACTOR = 100  ,SORT_IN_TEMPDB = OFF , IGNORE_DUP_KEY = OFF , STATISTICS_NORECOMPUTE = OFF , ONLINE = OFF , ALLOW_ROW_LOCKS = ON , ALLOW_PAGE_LOCKS = ON  )
	 ON [PRIMARY ]  GO;


-- ESIM.dbo.ITEMMOVTORETORNOBOLETO foreign keys

ALTER TABLE ESIM.dbo.ITEMMOVTORETORNOBOLETO ADD CONSTRAINT FK_ITEMMOVTRB_ITEMMOVTCB FOREIGN KEY (ITEMMOVTOCOBRANCAID) REFERENCES ESIM.dbo.ITEMMOVTOCOBRANCABOLETO(ITEMMOVTOCOBRANCAID) GO
ALTER TABLE ESIM.dbo.ITEMMOVTORETORNOBOLETO ADD CONSTRAINT FK_ITEMMOVTRB_ITEMMOVTR FOREIGN KEY (ITEMMOVTORETORNOID) REFERENCES ESIM.dbo.ITEMMOVTORETORNO(ITEMMOVTORETORNOID) GO;